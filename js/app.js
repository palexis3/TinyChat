import React from 'react';
import ReactDOM from 'react-dom';
import Login from './components/Login.js';

// render our initial component into DOM
ReactDOM.render(<Login />, document.getElementById('root'));
